/** @type {import('next').NextConfig} */
const nextConfig = {
    // images authorized host
    images: {
        domains: ['d36dz3u628srmn.cloudfront.net', 'oaidalleapiprodscus.blob.core.windows.net'],
    },
};

export default nextConfig;
